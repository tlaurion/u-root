diff -ruN /nix/store/aig3ajvcpp7j89zzj81cym15kxydgaa4-tinygo-0.40.1/share/tinygo/src/net/host_local.go /tmp/full_tgo/src/net/host_local.go
--- /nix/store/aig3ajvcpp7j89zzj81cym15kxydgaa4-tinygo-0.40.1/share/tinygo/src/net/host_local.go	1969-12-31 19:00:00.000000000 -0500
+++ /tmp/full_tgo/src/net/host_local.go	2026-06-28 20:51:24.751944839 -0400
@@ -0,0 +1,174 @@
+// TINYGO: Add missing net methods for host/Linux targets.
+// These are thin wrappers or stubs to allow u-root commands to compile.
+
+// Copyright 2026 the u-root Authors. All rights reserved
+// Use of this source code is governed by a BSD-style
+// license that can be found in the LICENSE file.
+
+//go:build !baremetal && !nintendoswitch && !wasm_unknown && !tinygo.wasm && !js
+
+package net
+
+import (
+	"errors"
+	"os"
+	"time"
+)
+
+// InterfaceByName returns the interface specified by name.
+func InterfaceByName(name string) (*Interface, error) {
+	return nil, errors.New("InterfaceByName not implemented")
+}
+
+// Addrs returns the interface's unicast interface addresses.
+func (ifi *Interface) Addrs() ([]Addr, error) {
+	return nil, errors.New("Addrs not implemented")
+}
+
+// FilePacketConn returns a copy of a packet connection's file descriptor.
+func FilePacketConn(f *os.File) (PacketConn, error) {
+	return nil, errors.New("FilePacketConn not implemented")
+}
+
+// ListenPacket announces on the local network address.
+func ListenPacket(network, address string) (PacketConn, error) {
+	return nil, errors.New("ListenPacket not implemented")
+}
+
+// Resolver looks up names and numbers.
+type Resolver struct {
+	// PreferGo controls whether Go's built-in DNS resolver is preferred
+	// on platforms where it's available.
+	PreferGo bool
+
+	// StrictErrors controls the behavior of temporary errors
+	// (including timeouts).
+	StrictErrors bool
+
+	// Dial optionally specifies an alternate dialer for use by
+	// Go's built-in DNS resolver to make TCP and UDP connections
+	// to DNS servers.
+	Dial func(ctx interface{}, network, address string) (Conn, error)
+}
+
+// LookupAddr performs a reverse lookup for the given address.
+func LookupAddr(addr string) (names []string, err error) {
+	return nil, errors.New("LookupAddr not implemented")
+}
+
+// LookupHost looks up the given host using the local resolver.
+func LookupHost(host string) (addrs []string, err error) {
+	return nil, errors.New("LookupHost not implemented")
+}
+
+// LookupCNAME returns the canonical name for the given host.
+func LookupCNAME(host string) (cname string, err error) {
+	return "", errors.New("LookupCNAME not implemented")
+}
+
+// UnixConn is an implementation of the Conn interface for connections
+// to Unix domain sockets.
+type UnixConn struct {
+	conn
+
+	fd    int
+	net   string
+	laddr *UnixAddr
+	raddr *UnixAddr
+}
+
+func (c *UnixConn) Read(b []byte) (int, error) {
+	return 0, errors.New("UnixConn.Read not implemented")
+}
+
+func (c *UnixConn) Write(b []byte) (int, error) {
+	return 0, errors.New("UnixConn.Write not implemented")
+}
+
+func (c *UnixConn) Close() error {
+	return errors.New("UnixConn.Close not implemented")
+}
+
+func (c *UnixConn) LocalAddr() Addr {
+	return c.laddr
+}
+
+func (c *UnixConn) RemoteAddr() Addr {
+	return c.raddr
+}
+
+func (c *UnixConn) SetDeadline(t time.Time) error {
+	return errors.New("UnixConn.SetDeadline not implemented")
+}
+
+func (c *UnixConn) SetReadDeadline(t time.Time) error {
+	return errors.New("UnixConn.SetReadDeadline not implemented")
+}
+
+func (c *UnixConn) SetWriteDeadline(t time.Time) error {
+	return errors.New("UnixConn.SetWriteDeadline not implemented")
+}
+
+// UnixListener is a Unix domain socket listener.
+type UnixListener struct {
+	fd    int
+	laddr *UnixAddr
+}
+
+func (l *UnixListener) Accept() (Conn, error) {
+	return nil, errors.New("UnixListener.Accept not implemented")
+}
+
+func (l *UnixListener) Close() error {
+	return errors.New("UnixListener.Close not implemented")
+}
+
+func (l *UnixListener) Addr() Addr {
+	return l.laddr
+}
+
+// ListenUnix announces on the local Unix domain socket.
+func ListenUnix(network string, laddr *UnixAddr) (*UnixListener, error) {
+	return nil, errors.New("ListenUnix not implemented")
+}
+
+// ListenUnixgram announces on the local Unix domain socket.
+func ListenUnixgram(network string, laddr *UnixAddr) (*UnixConn, error) {
+	return nil, errors.New("ListenUnixgram not implemented")
+}
+
+// DialUnix connects to a Unix domain socket.
+func DialUnix(network string, laddr, raddr *UnixAddr) (*UnixConn, error) {
+	return nil, errors.New("DialUnix not implemented")
+}
+
+// CloseWrite shuts down the writing side of the Unix domain connection.
+func (c *UnixConn) CloseWrite() error {
+	return errors.New("UnixConn.CloseWrite not implemented")
+}
+
+// CloseRead shuts down the reading side of the Unix domain connection.
+func (c *UnixConn) CloseRead() error {
+	return errors.New("UnixConn.CloseRead not implemented")
+}
+
+// LookupHost looks up the given host using the local resolver.
+func (r *Resolver) LookupHost(ctx interface{}, host string) (addrs []string, err error) {
+	return nil, errors.New("Resolver.LookupHost not implemented")
+}
+
+// LookupAddr looks up the given address using the local resolver.
+func (r *Resolver) LookupAddr(ctx interface{}, addr string) (names []string, err error) {
+	return nil, errors.New("Resolver.LookupAddr not implemented")
+}
+
+// LookupPort looks up the port for the given network and service.
+func (r *Resolver) LookupPort(ctx interface{}, network, service string) (port int, err error) {
+	return 0, errors.New("Resolver.LookupPort not implemented")
+}
+
+// ListenIP acts like ListenPacket for IP networks.
+func ListenIP(network string, laddr *IPAddr) (*IPConn, error) {
+	return nil, errors.New("ListenIP not implemented")
+}
+
diff -ruN /nix/store/aig3ajvcpp7j89zzj81cym15kxydgaa4-tinygo-0.40.1/share/tinygo/src/net/net.go /tmp/full_tgo/src/net/net.go
--- /nix/store/aig3ajvcpp7j89zzj81cym15kxydgaa4-tinygo-0.40.1/share/tinygo/src/net/net.go	1969-12-31 19:00:01.000000000 -0500
+++ /tmp/full_tgo/src/net/net.go	2026-06-28 20:14:21.329334524 -0400
@@ -302,6 +302,13 @@
 	return s
 }
 
+// InvalidAddrError is an error type representing an invalid address.
+type InvalidAddrError string
+
+func (e InvalidAddrError) Error() string { return string(e) }
+func (e InvalidAddrError) Timeout() bool   { return false }
+func (e InvalidAddrError) Temporary() bool { return false }
+
 // errNetClosing is the type of the variable ErrNetClosing.
 // This is used to implement the net.Error interface.
 type errNetClosing struct{}
diff -ruN /nix/store/aig3ajvcpp7j89zzj81cym15kxydgaa4-tinygo-0.40.1/share/tinygo/src/net/tcpsock.go /tmp/full_tgo/src/net/tcpsock.go
--- /nix/store/aig3ajvcpp7j89zzj81cym15kxydgaa4-tinygo-0.40.1/share/tinygo/src/net/tcpsock.go	1969-12-31 19:00:01.000000000 -0500
+++ /tmp/full_tgo/src/net/tcpsock.go	2026-06-28 20:14:21.325334562 -0400
@@ -12,6 +12,7 @@
 	"internal/itoa"
 	"io"
 	"net/netip"
+	"os"
 	"strconv"
 	"syscall"
 	"time"
@@ -282,6 +283,24 @@
 	return fmt.Errorf("CloseWrite not implemented")
 }
 
+// File returns a copy of the underlying file descriptor.
+func (c *TCPConn) File() (*os.File, error) {
+	return nil, errors.New("TCPConn.File not implemented")
+}
+
+// KeepAliveConfig contains TCP keep-alive options.
+type KeepAliveConfig struct {
+	Enable   bool
+	Idle     time.Duration
+	Interval time.Duration
+	Count    int
+}
+
+// SetKeepAliveConfig configures keep-alive messages sent by the operating system.
+func (c *TCPConn) SetKeepAliveConfig(config KeepAliveConfig) error {
+	return errors.New("TCPConn.SetKeepAliveConfig not implemented")
+}
+
 type listener struct {
 	fd    int
 	laddr *TCPAddr
diff -ruN /nix/store/aig3ajvcpp7j89zzj81cym15kxydgaa4-tinygo-0.40.1/share/tinygo/src/net/udpsock.go /tmp/full_tgo/src/net/udpsock.go
--- /nix/store/aig3ajvcpp7j89zzj81cym15kxydgaa4-tinygo-0.40.1/share/tinygo/src/net/udpsock.go	1969-12-31 19:00:01.000000000 -0500
+++ /tmp/full_tgo/src/net/udpsock.go	2026-06-28 20:14:21.325334562 -0400
@@ -206,6 +206,30 @@
 	return nil, errors.New("SyscallConn not implemented")
 }
 
+// ListenUDP acts like ListenPacket for UDP networks.
+func ListenUDP(network string, laddr *UDPAddr) (*UDPConn, error) {
+	switch network {
+	case "udp", "udp4", "udp6":
+	default:
+		return nil, UnknownNetworkError(network)
+	}
+	fd, err := netdev.Socket(_AF_INET, _SOCK_DGRAM, _IPPROTO_UDP)
+	if err != nil {
+		return nil, err
+	}
+	laddrport := laddr.AddrPort()
+	err = netdev.Bind(fd, laddrport)
+	if err != nil {
+		netdev.Close(fd)
+		return nil, err
+	}
+	return &UDPConn{
+		fd:    fd,
+		net:   network,
+		laddr: laddr,
+	}, nil
+}
+
 // TINYGO: Use netdev for Conn methods: Read = Recv, Write = Send, etc.
 
 func (c *UDPConn) Read(b []byte) (int, error) {
@@ -234,7 +258,28 @@
 
 // ReadFrom implements the PacketConn ReadFrom method.
 func (c *UDPConn) ReadFrom(b []byte) (int, Addr, error) {
-	return 0, nil, errors.New("ReadFrom not implemented")
+	n, err := netdev.Recv(c.fd, b, 0, c.readDeadline)
+	if n < 0 {
+		n = 0
+	}
+	if err != nil && err != io.EOF {
+		err = &OpError{Op: "read", Net: c.net, Source: c.laddr, Addr: c.raddr, Err: err}
+	}
+	return n, c.raddr, err
+}
+
+// ReadFromUDP reads a UDP packet from c, copying the payload into b.
+// It returns the number of bytes read and the remote address.
+func (c *UDPConn) ReadFromUDP(b []byte) (int, *UDPAddr, error) {
+	n, addr, err := c.ReadFrom(b)
+	if err != nil {
+		return 0, nil, err
+	}
+	ua, ok := addr.(*UDPAddr)
+	if !ok {
+		return 0, nil, &OpError{Op: "readfrom", Net: c.net, Source: c.laddr, Addr: c.raddr, Err: errors.New("unexpected address type")}
+	}
+	return n, ua, err
 }
 
 // ReadMsgUDP reads a message from c, copying the payload into b and
@@ -251,7 +296,19 @@
 
 // WriteTo implements the PacketConn WriteTo method.
 func (c *UDPConn) WriteTo(b []byte, addr Addr) (int, error) {
-	return 0, errors.New("WriteTo not implemented")
+	n, err := netdev.Send(c.fd, b, 0, c.writeDeadline)
+	if n < 0 {
+		n = 0
+	}
+	if err != nil {
+		err = &OpError{Op: "write", Net: c.net, Source: c.laddr, Addr: addr, Err: err}
+	}
+	return n, err
+}
+
+// WriteToUDP writes a UDP packet to addr via c, returning the number of bytes written.
+func (c *UDPConn) WriteToUDP(b []byte, addr *UDPAddr) (int, error) {
+	return c.WriteTo(b, addr)
 }
 
 // WriteMsgUDP writes a message to addr via c if c isn't connected, or

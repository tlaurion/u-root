--- a/src/crypto/tls/tls.go
+++ b/src/crypto/tls/tls.go
@@ -118,3 +118,12 @@
 func LoadX509KeyPair(certFile, keyFile string) (Certificate, error) {
 	return Certificate{}, errors.New("tls:LoadX509KeyPair not implemented")
 }
+
+// Conn represents a TLS connection.
+// Type alias to net.TLSConn for compatibility with Go's crypto/tls API.
+type Conn = net.TLSConn
+
+// Listen creates a TLS listener accepting connections from the provided Listener.
+func Listen(network, laddr string, config *Config) (net.Listener, error) {
+	return nil, errors.New("tls.Listen not implemented")
+}

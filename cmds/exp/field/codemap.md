# cmds/exp/field/

## Responsibility
Reads newline-separated data, splits into fields by regex, and prints selected field ranges (similar to awk field selection).

## Integration Points
- (none; uses only standard library)

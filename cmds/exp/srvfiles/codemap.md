# cmds/exp/srvfiles/

## Responsibility
Simple static file HTTP server that serves a directory over the network with cache-control disabled.

## Integration Points
- (uses only standard library net/http)

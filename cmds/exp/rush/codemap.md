# cmds/exp/rush/

## Responsibility
An interactive shell similar to sh, with built-in commands and pipeline support.

## Integration Points
- (uses only standard library; built-in command implementations in the package)

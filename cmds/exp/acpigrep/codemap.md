# cmds/exp/acpigrep/

## Responsibility
Filters a stream of ACPI tables by signature regexp (like grep for ACPI tables).

## Integration Points
- pkg/acpi: used for parsing raw ACPI table data from stdin

# cmds/core/ts/

## Responsibility
Prepend each line of stdin with a timestamp, with relative timestamp options.

## Integration Points
- `pkg/ts`: Timestamp formatting
- `pkg/uroot/unixflag`: UNIX-style flag conversion

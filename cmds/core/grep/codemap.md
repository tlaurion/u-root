# cmds/core/grep/

## Responsibility
Search file contents using regular expressions or fixed strings, with recursive directory search and line numbering.

## Integration Points
- `pkg/uroot/unixflag`: UNIX-style flag conversion
